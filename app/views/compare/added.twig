<div id="shopmcart-modal" class="popup-box mcart-popup-box mfp-content">
    <div class="title"><h3>{{ t('app', 'Товар добавлен в список сравнения') }}</h3></div>
    <div class="clearfix">
        <a onclick="$.magnificPopup.close()" class="left btn3">{{ t('app', 'Продолжить просмотр') }}</a>
        <a href="{{ compare.url }}" class="right btn no-arrow">{{ t('app', 'Сравнить товары') }}</a>
    </div>
</div>
